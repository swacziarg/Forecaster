# Focused launch finishing pass — September 6, 2026

The user authorized starting the next Luna workers and launch lead. This continues the existing shared local Forecast tasks; it does not authorize publishing, deploying, or inventing editorial approval. Work in `/Users/simonwacziarg/Documents/ChatGPT/Forecast`.

Read `docs/luna-launch/README.md` for evidence and immutable-history rules and `docs/launch-work/launch-readiness.md` for the last result. This pass replaces the earlier parallel ownership map and completion receipts. Five workers run concurrently; the launch lead reviews and integrates their stable handoffs, then performs browser QA. You are not alone in the checkout. Preserve all unrelated changes; do not switch branches, reset, commit, push, deploy, launch extra tasks, or edit another worker's files.

## Exclusive ownership

- `interface`: `src/DailyGame.tsx`, `src/App.tsx` for a needed integration fix only, `package.json`, a new focused UI integration test if useful, and `docs/launch-work/finish-pass/interface/`. Handle the new stale-draft response, reuse the centralized share payload, register routing/readiness tests in the standard test command, and investigate resulting behavior carefully. Do not modify domain modules or existing domain tests.
- `temporal`: `src/domain/study.ts`, `src/domain/dailyGame.ts`, `src/domain/eventStudy.test.ts`, `src/domain/dailyGame.test.ts`, and `docs/launch-work/finish-pass/temporal/`. Investigate whether date-only/unknown source publication values incorrectly pass precise pre-reveal cutoffs. Implement an honest, backward-compatible timing representation/eligibility rule with meaningful tests. No invented midnight public times, global weakening of evidence rules, or silent mutation of published data. Preserve public scoring/storage APIs. Do not edit study data modules: provide an exact migration handoff for the lead. If stricter eligibility makes new drafts fail, disclose it rather than exempting them to make tests pass.
- `tiktok`: `docs/launch-work/finish-pass/tiktok/` only. Read existing TikTok packets and source captures, then produce a versioned evidence addendum with better contemporary public-time support for each affected claim. Distinguish original public facts from archive upload times and editable pages. Preserve prior packets. Do not redo price research or tune anchors; freeze any evidence-driven correction before rescoring through unchanged measurement functions. Deliver a precise source/claim/runtime-field patch specification and an updated reviewable candidate artifact, or exact remaining unavailable evidence and attempted retrievals.
- `eagles`: `docs/launch-work/finish-pass/eagles/` only. Resolve gamebook occurrence versus public availability and recap update cutoffs using retained official evidence plus targeted contemporary captures. Preserve the five-card selection, combined injury/loss, playoff confounds, dataset, and prior packets. Deliver a versioned evidence addendum, updated reviewable candidate, and exact runtime field changes; freeze justified timing corrections before recalculation. Do not substitute event dates for source-publication instants.
- `canada`: `docs/launch-work/finish-pass/canada/` only. Close the narrow known reserve defects: unusable Axios challenge capture, unknown-time retrospective Elections Canada material, and assessment of the three-card tie. Replace or remove unusable evidence using legitimate primary/corroborating captures and retain precise receipts; keep retrospective information out of pre-reveal claims. No post-score event swaps. Deliver an updated candidate/addendum or a specific hold. This reserve cannot block finishing the primary two editions.
- `lead`: `docs/launch-work/finish-pass/coordination.md`, review records inside that directory while workers run; afterward integrate handed-off corrections into new draft study modules/tests, schedule proposals and global docs, run final tests/build/browser QA, and update `docs/launch-work/launch-readiness.md`. Do not edit producer packets or take ownership before final handoff.

Each role owns `docs/launch-work/finish-pass/status/<role>.json`. Write running status at start, actual UTC time, actual task ID if available, and owned paths. Write your report/artifact hashes first, then the final receipt LAST with `status: complete` or `blocked`, `handoffComplete: true`, report path, files changed, checks, precise blockers, and finishedAt. A blocked receipt means the bounded investigation is complete, not that other tasks should stop. Do not touch earlier pass receipts.

## Checks and handoffs

Workers run focused tests only. One worker's stricter temporal checks may reveal draft failures another worker cannot fix within ownership; report them for lead integration. The lead alone owns production builds and browser QA in this pass. API proposals go in each worker's report, not in other workers' files. Do not assume the Mac remains locked: the lead should check currently available browser tools and attempt real browser rehearsal when implementation is stable.

Evidence workers deliver concrete remediation, not just the same generic request for human approval. Actual scientific uncertainty must remain explicit; human approval cannot transform an unknown posting time into verified fact. A conservative public-by bound can be used only with supporting evidence and clear semantics. The lead reviews exact new artifacts independently before changing candidate verdicts; substantive revisions invalidate prior hashes/approval proposals.

## Lead finish criteria

Wait on the five actual worker task IDs provided in the dispatch message, using compact task-wait snapshots/cursors and bounded waits. Do not respawn duplicate workers. While waiting, read existing review material, inspect the readiness checker, and prepare a final QA matrix. Independently review completed evidence addenda as they arrive. After all five final handoffs, integrate eligible updates, fix cross-owner issues, and run all registered tests, production build, and the readiness checker. Test date-only source rejection, stale/newer draft restoration, share/copy payloads, routing/draft states, and real-browser mobile/desktop gameplay, refresh, two-tab persistence, errors, and a deterministic daily boundary. Use isolated test attempts/local previews; preserve real user records and the live schedule.

Refresh the concrete TikTok/Eagles approval packet and proposed release dates if warranted, then present exactly what editorial decisions remain and whether Canada is a viable reserve. Do not ask the user for vague approval before doing the authorized implementation/evidence/browser work. Do not publish or claim the new content is ready if source eligibility is still unresolved. Finish with exact evidence-based status, not just 'waiting'. No production release is authorized by launching these tasks.
