# Parallel worker: Readiness tools and release operations

Read `docs/luna-launch/parallel/README.md` and follow its worker protocol as role `operations`.

Start `docs/luna-launch/11-content-and-release-operations.md` immediately with these parallel boundaries. Do not wait for a final schedule: build the readiness tool against the current registry and explicit fixtures. Accept absent approval/schedule packets as real missing inputs, not exceptions that crash the command. The lead connects final artifacts later.

Own the new readiness script and its own tests, additive package.json script changes, `docs/launch-work/operations/`, `index.html` and static metadata/assets only when a verified correction is needed, completion 11, and receipt. Do not edit README, HANDOFF, hosting documentation, registries, domain modules/tests, study modules, or Wrangler configuration in the parallel phase. Draft proposed shared-document updates inside the operations directory for the lead.

Coordinate the readiness-report input format through a documented schema in your directory; do not depend on another worker's unfinished JSON. Use actual validators and make checks read-only. Preserve the historical #001 approval treatment and distinguish proposed from approved runway. Do not install dependencies, rewrite the lockfile, or add CI automation merely to register the command.

You own full build/dry-run checks during this phase. Because UI/domain files may change, record the observed revision/time and treat results as provisional; the lead reruns them on stable final files. Report unrelated concurrent failures without editing another worker's files. Do not manipulate the shared browser or deploy.

Deliver the tool, targeted tests, operations guide, integration/schema notes, metadata/package verification, and drafted shared-doc updates. Then write your final receipt and stop editing.
