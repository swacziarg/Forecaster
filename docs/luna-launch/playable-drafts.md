# Finish six playable editions — September 7, 2026

The user explicitly said: "ok so do whats next with luna" after being told Oscars, Bitcoin, and Canada still need to become tested playable drafts and that the lead incorrectly deferred authorized implementation behind approval. Execute that work now. Human editorial approval gates publication, not local draft implementation, packaging, calculations, tests, or private preview. This instruction supersedes prior prompt text requiring approval before draft registration or packaging.

Work in the shared `/Users/simonwacziarg/Documents/ChatGPT/Forecast` checkout. Read existing candidate packets, current Study temporal semantics, LocalDatasetAdapter, and existing draft integration tests. Preserve original study versions, datasets, frozen selections, source captures, published puzzle identities and saved attempts. You are not alone in this codebase: respect ownership, accommodate other changes, never revert them, and do not branch-switch, reset, commit, push, deploy, or start more tasks. The launch remains Biden September 7, TikTok September 8, Eagles September 9, Oscars September 10, Bitcoin September 11, Canada September 12, Trump optional September 13. These are content dates; only actual approved content may be published. Check actual clock; never shift the user's dates automatically because preparation took time.

## Concurrent worker ownership

Each worker owns its unique runtime draft module, a corresponding unique standalone test, unique versioned data bundle if needed, its report/evidence under `docs/launch-work/playable-drafts/<role>/`, and its own status receipt. Workers do NOT edit `src/data/studies.ts`, `package.json`, shared integration tests, domain modules, scheduling, UI, global docs, or any other worker's files. The lead integrates those after handoff. Existing unchanged immutable data may be reused with exact hash checks; new bundles go only to the specified new paths.

- `oscars`: own `src/data/oscarsFiveCard2026.ts`, `src/data/oscarsFiveCard2026.test.ts`, and `public/data/oscars-best-picture-2026-v2/` if a new bundle is actually needed. Implement a strict version-2 `editorial-review` Study from `docs/launch-work/funnel-expansion/oscars/candidate.json`; assign a unique slug that cannot collide with the existing v1 archive. Reuse exact current market dataset only where identity/bytes are unchanged. Check all claimed source availability against current validator semantics, actual passages, timezone and date precision. Resolve Academy start/completion evidence honestly; a scheduled start is not proof that every nomination was known. Preserve neutral candidate JSON claims; the prior worker's report section called "Neutral card copy" accidentally included market levels, which must NEVER enter pre-reveal copy. Do not tune ties or scoring thresholds. Return calculated groups under the existing comparator and distinguish example order agreement from comparable-pair count. Complete private playable draft inputs and tests; no publication approval prerequisite.
- `bitcoin`: own `src/data/bitcoinFiveCard2024.ts`, `src/data/bitcoinFiveCard2024.test.ts`, and `public/data/bitcoin-100k-2024-v2/` as needed. Implement a distinct five-card v2 draft without overwriting the existing eight-event v1. Resolve the fullMarketLifetime discrepancy from actual provider coverage and record truthful metadata in the new version. Obtain legitimate source-time evidence for HKEX, Mt. Gox and SEC/MicroStrategy anchors; do not convert unknown publication to midnight or grandfather a new draft into legacy evidence exceptions. Use primary archives or contemporary corroboration for defensible public-by bounds; freeze any justified anchor correction before recalculation. Report actual overlap rather than adding a blanket no-overlap test. Build source-neutral five-card copy and independent measured answer/ties from prediction probabilities, not spot prices. Complete module/data/tests or identify a specific demonstrated fact that truly prevents one card from being eligible after exhausting practical retrieval alternatives.
- `canada`: own `src/data/canada2025.ts`, `src/data/canada2025.test.ts`, and `public/data/canada-liberal-comeback-v2/`. Consolidate the existing v2 candidate and retained corrected AP evidence into a strict draft and reproducible public dataset bundle. Preserve primary source occurrence roles without pretending date-only records establish an exact minute. Confirm each pre-reveal claim is source-eligible; unknown-time retrospective materials stay out. Keep the supported three-card tie and explain seven comparable pairs plainly; tie preference does not block implementation. Include the sourced reveal-only ending. Produce module/data/tests ready for lead registration.
- `trump`: own only `docs/launch-work/playable-drafts/trump/` and your receipt. Finish the omitted descriptive scoring/response packet using retained YES history, frozen five cards, and the current evidence-corrected anchors. Calculate existing engine reference/stabilized/delayed values, all window checks, tie groups, and comparable pairs. This historical descriptive calculation is in scope; do not invent a prohibition on calculating political-market scores. Retain no-causality, uncertainty, and temporal-evidence distinctions. Audit the revised approximate debate bound: an approximate end cannot be rounded earlier without justification. Preserve original anchor/selection and write evidence-driven corrections before recalculating. Provide a complete optional-buffer integration handoff, not null scores and another generic approval request. No runtime/registry edits in this worker.

## Worker completion

At start write `docs/launch-work/playable-drafts/status/<role>.json` with actual task ID/time, status running and owned files. Do not reuse earlier pass receipts. Keep progress in your own report. Implement rather than merely proposing. Validate against current strict Study rules and getPreRevealCard for all five events. Verify local source and dataset hashes, exact market/side, all 25 required windows, unchanged normalization, tie/scoring behavior, ending exclusion, and source-cutoff failure cases. Add a focused standalone test runnable with the project's Node type-stripping convention. Tests may run directly without changing package.json. Avoid production builds while other workers edit; the lead owns final builds/browser QA.

Write final report with exported symbol, ID/version/slug, paths, tests/results, current hashes, source/cutoff correction log, proposed initial order and remaining precise editorial decisions. Then final receipt LAST: status complete or blocked, handoffComplete true, finishedAt, reportPath, exact files/hashes. Stop editing after handoff. A genuine unavailable source may prevent a particular card; a generic lack of human approval may not prevent draft work.

## Launch lead — mandatory completion, not another proposal-only pass

1. Coordinate the four actual task IDs supplied at dispatch. Read current status, use compact task waits/cursors with bounded waits, and work on independent review/preview design while waiting. Do not rely on old receipts. Do not author replacement evidence and claim independent review of your own additions.
2. As producers hand off, independently audit their source/temporal claims, spoiler firewall, dataset identity and calculations. Correct objective integration issues after handoff with clear logs. Preserve prior versions. Review Trump as an optional buffer; it cannot delay primary six integration.
3. Register the eligible Oscars/Bitcoin/Canada draft modules in `src/data/studies.ts`, with unique slugs, and their tests in npm test. Update shared integration tests without hardcoding every new study to v1 or forbidding legitimate disclosed overlaps. Preserve existing fixtures and published daily behavior. Human approval is NOT required for these local draft registrations.
4. Make all five new topics privately playable in the local browser before approval, including the real five-card daily layout, reveal, ties, ending and source details. Existing production routes correctly shield drafts; implement a minimal development-only preview path guarded by `import.meta.env.DEV` or an equivalent non-production mechanism. Give explicit topic URLs, isolated preview puzzle IDs/storage, and no official streak credit. Do not publish draft routes by changing status, exposing future daily registry entries, or weakening production shielding. Avoid new dependencies or a CMS. Test production cannot activate the preview with a query parameter.
5. Run every test, type check, build, readiness command, and actual browser gameplay for Oscars/Bitcoin/Canada at mobile and desktop dimensions. Also verify existing Biden and TikTok/Eagles previews, old archive result isolation, unknown/future links, no pre-reveal market moves, saved attempts/share text, and production draft shielding. Use available browser skills/tools; don't assume a historical locked-Mac error persists. Record unverified platform cases honestly.
6. Final handoff must include exact working private preview URLs and a concrete approval packet for the six-topic dated lineup, with hashes, plain-language timing/overlap/tie caveats, and per-topic source review. Update launch-readiness.md and funnel.md to distinguish research packets, implemented playable drafts, approved schedules, and deployment. Do not claim all six playable if a source failure prevented integration; explain the precise missing card and the best available buffer instead.
7. The user already set dates/cadence and delegated later topic choice. Do not demand another start-date decision or treat six dates as six finished editions. Approval is the FINAL content decision after authorized preparation; deployment is still not authorized by this request. Do not commit, push, or deploy. Finish all independently actionable work rather than ending with 'please approve so I can build'.
