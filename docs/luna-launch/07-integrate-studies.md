# 07 — Integrate the completed candidates as local studies

Read `docs/luna-launch/README.md`, candidate packets, and task 06 verdicts. Implement complete validated candidates; retain editorial-review status unless actual human approval for the exact artifacts is recorded.

## Ownership

Own new per-topic modules under `src/data/`, their versioned `public/data/` artifacts, necessary `src/data/studies.ts` additions, focused dataset/study tests, provenance documentation, and completion `07.md`. Preserve old study modules and published versions. Do not edit daily scheduling or redesign the UI in this task.

## Execute

1. Map each complete candidate to the strict Study schema. Resolve review corrections only from evidence, retaining a change log and sending substantive content/anchor changes back through task 06. If a required fact is unknown, leave that candidate unregistered and identify the missing field. Do not weaken validation to make it compile.
2. Create new strict five-card identities for TikTok, Eagles, and Trump. Keep the existing eight-event Eagles and ten-event election studies intact. Give any accepted culture/Canada reserve an unambiguous versioned identity as well; do not register blocked placeholders.
3. Package normalized prices, retained raw provider responses, metadata, and manifest into reproducible local datasets compatible with `LocalDatasetAdapter`. Preserve exact hashes and normalization provenance. Reuse an existing immutable dataset only if its exact identity and bytes match the candidate; otherwise create a new version without replacing the old one.
4. Register valid studies as `editorial-review` by default. Confirm publication-gated surfaces do not accidentally advertise drafts. Keep all prices local at runtime and keep endings/retrospective content out of pre-reveal claims.
5. Add focused coverage for exact contract/dataset identity, artifact digests, source cutoff rejection, all selected windows, stable golden response/tie results, and study registration. Derive golden values independently from the reviewed packet, not by blindly copying output from changed code.
6. Run the full application tests and build. Verify old election and Biden fixtures unchanged. Update provenance with full hashes, observed coverage, rejected observations, and limitations. Report available draft routes for final browser QA.

## Done when

Every eligible candidate loads from a strict local snapshot and reproduces reviewed calculations, all existing checks pass, and missing approvals remain honestly labeled. Emit a table of study ID/version, dataset hash, editorial state, and candidate/review artifact hashes for task 08.
