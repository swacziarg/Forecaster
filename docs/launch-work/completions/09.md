# Task 09 completion — player experience

Date: 2026-09-06
Worker: interface

## Files changed

- `src/App.tsx`
- `src/DailyGame.tsx`
- `src/domain/launchRouting.ts`
- `src/domain/launchRouting.test.ts`
- `docs/launch-work/ux/report.md`
- `docs/launch-work/completions/09.md`
- `docs/launch-work/parallel-status/interface.json`

## Complete

The released daily flow was checked through movement, inline details, reveal, score/comparison/takeaway, copy feedback, and refresh restoration. Required viewport checks showed no horizontal overflow and no obscured reveal control. Public routing now distinguishes published, editorial-review, and unknown study paths. Pre-reveal expected-direction hints are hidden on the legacy study surface. Loading/error copy is truthful and safe, and methodology/daily dialogs have contained focus, Escape handling, and return-focus behavior.

## Checks actually run

- `npm test` — passed.
- `node --experimental-strip-types src/domain/launchRouting.test.ts` — passed.
- `npx tsc -b --pretty false` — passed.
- `git diff --check` — passed.
- Interactive browser evidence and route/viewport observations are recorded in `docs/launch-work/ux/report.md`.
- Full production build — unverified by this worker because parallel protocol delegates full builds to operations and the lead.

## Remaining blockers and requests

- Register the new routing test in the package test command during final integration.
- Final candidate-card headline checks, real-device touch checks, first-time-player timing, native share-sheet checks, and full production build remain for the lead/operations pass.
- No interface-owned domain/API request is outstanding.

## Next task inputs

Lead can review the route helper/test and UX report, then integrate the interface changes after reliability/operations handoffs. Preserve the existing daily puzzle IDs, study data, scoring, storage semantics, and exact archive links.
