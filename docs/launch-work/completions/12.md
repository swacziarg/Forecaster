# Completion 12 — Final launch rehearsal and release decision

Date: 2026-09-06
Task: Final funnel-expansion verification.

## Decision

The local launch preparation is blocked pending exact human approvals and candidate-specific integration gates. The requested six-distinct-edition funnel is prepared as a concrete proposal, but only Biden #002 is approved and no later entry is live.

## Verified content state

- #001 Sep 5 Biden archive is preserved; #002 Sep 7 Biden launch has exact approval and isolated identity.
- #003 TikTok and #004 Eagles are integrated editorial-review drafts.
- #005 Oscars, #006 Bitcoin, and #007 Canada are current strict evidence packets with explicit remaining decisions; #008 Trump is an optional review-only buffer.
- The complete current funnel is recorded in the funnel table and proposal queue.

## Checks

- npm test — passed.
- npx tsc -b --pretty false — passed.
- npm run build — passed.
- npm run build:cloudflare — passed.
- git diff --check — passed.
- npm run check:launch-readiness -- --now 2026-09-06T20:41:51Z --json — exit 1 as intended: 1 distinct approved / 6 target, 0 approved upcoming, 6 proposed upcoming; validators and artifacts clean.
- npx wrangler deploy --dry-run — exit 0, read 52 dist files, no bindings, no deployment; known local Wrangler log EPERM warning.
- Production-build browser at http://127.0.0.1:5180/ — passed archive play, movement, source detail, reveal/score/chart, copy feedback, refresh persistence, published-only index, draft shielding, future/unknown links, and clean console.
- Deterministic schedule checks passed before/at/after Sep 7 and preserved archive mode. Prior finish-pass responsive matrix covered 390×844 and 1280×900; this funnel pass changed no UI code.

## Remaining actions

1. Obtain exact human approvals for later proposals.
2. Resolve Oscars, Bitcoin, and Canada candidate-specific gates before runtime registration.
3. Keep Trump out of the six-edition core unless its remaining gates close.
4. After approvals, refresh moved dates if needed, register only approval-backed entries, rerun all checks, and request separately authorized deployment.

No push, merge, deployment, remote configuration change, external announcement, human playtest, native share-sheet verification, or remote HTTPS verification was performed.

