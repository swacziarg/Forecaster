# Thunder announcement evidence repair

This packet repairs the timing and headline evidence for two Thunder developments. The retained NBA pages are copied as byte-preserving snapshots with SHA-256 hashes. The fracture cutoff uses the NBA article's JSON-LD publication instant because the linked team announcement could not be independently fetched in this environment. The return cutoff uses Chet Holmgren's own embedded tweet timestamp decoded from its Snowflake ID. The return headline means the February 6 announcement; the February 7 Toronto game remains a separate result.
